# Signing key — losing this means losing the ability to publish updates to
# the same app on Play Store. Back it up separately, never commit it.
android.keystore
*.keystore
*.jks

# Build outputs (regenerated by `bubblewrap build`)
*.apk
*.apk.idsig
*.aab
app/build/
build/
.gradle/
